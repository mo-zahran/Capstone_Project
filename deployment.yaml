# apiVersion: v1
# kind: Deployment
# metadata:
#  name: sample-site
#  labels:
#    app: nginx
# spec:
#  containers:
#    - name: pod-node-server
#      image: asmirjahic/capstone
#      ports:
#        - containerPort: 80

apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
  labels:
    app: nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: asmirjahic/capstone
        ports:
        - containerPort: 80